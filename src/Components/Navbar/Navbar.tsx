// Moved Navbar component here
import React, { useState, useEffect, useRef } from 'react';
import { Link } from 'react-router-dom';
import { useNavbar } from '../../utils/Context';

const megaMenus = {
  // ...existing code...
};

const navItems = ["Services", "Portfolio", "Resources", "Company"];

const Navbar = () => {
  // ...existing code...
  return (
    <header>
      {/* ...existing code... */}
    </header>
  );
};

export default Navbar;
